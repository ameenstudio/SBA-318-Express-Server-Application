//import
import express from 'express';

//Setup
const app = express ();
const PORT = 3000 || 3001

// Middelware

//Routes

//Listener 
app.listen(PORT, ()=>{

    console.log(`server runing op Port: ${PORT}`)
    
    })